========================================
📤 快速上传到 GitHub
========================================

方法 1: 使用脚本（推荐）
------------------------
./upload_to_github.sh

方法 2: 手动执行
------------------------
# 1. 初始化
git init
git add .
git commit -m "Initial commit: 小红书自动化工具"

# 2. 在 GitHub 创建仓库
访问: https://github.com/new
仓库名: xhs-automation

# 3. 连接并推送
git remote add origin https://github.com/你的用户名/xhs-automation.git
git branch -M main
git push -u origin main

========================================
⚠️  重要提醒
========================================

上传前请确认：
✅ .env 文件不会被上传（已在 .gitignore）
✅ config.yaml 不会被上传（已在 .gitignore）
✅ API Key 等敏感信息已移除

检查命令：
git status | grep -E "\.env|config\.yaml"

如果看到这些文件，说明配置有问题！

========================================
📚 详细说明
========================================

查看完整指南: cat 上传到GitHub指南.md
